"""Shared utilities used across multiple Avis surfaces.

Modules here are dependency-light helpers that don't fit cleanly
into one domain package (``agent``, ``vision``, ``audio``,
``fusion``, ``notify``, ``data``, ``web``). Importable from both
``src/`` packages and ``tools/`` callers.
"""
